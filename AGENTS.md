# AGENTS.md

このファイルは、このリポジトリで作業する AI/自動化エージェント向けの運用ルールです。
スコープ: リポジトリ全体（このファイル配下のすべて）

## 1. プロジェクト概要
- アプリ名: QUALog (Beta)
- 目的: 資格学習の目標・進捗・振り返りを支援する学習ログアプリ
- 主な技術:
  - Frontend: Next.js (App Router), TypeScript
  - Styling: Tailwind CSS
  - Hosting/Auth: AWS Amplify Gen2 + Cognito
- プロダクト哲学:
　- 学習継続の心理負荷を下げることを優先
　- 高機能化より、迷わないUIを優先
　- 「毎日少しでも続けられる」体験を重視
　- 学習記録入力は3タップ以内を理想とする
　- 達成感を感じるUIを意識

## 2. 作業前チェック
変更前に以下を確認すること:
1. 影響範囲のファイルを特定する
2. 既存実装の命名規則・構造を踏襲する
3. UI変更時はアクセシビリティ（aria属性/キーボード操作）を壊さない

## 3. 実装方針
- 最小変更を優先（不要なリネーム・大規模整形はしない）
- 1つのPR/コミットで1つの目的に絞る
- サーバー処理とUI処理の責務を分離する
- `any` の安易な使用は禁止（必要なら理由をコメント）

## 4. コードスタイル
- TypeScriptの型を明示し、推論任せにしすぎない
- 既存のESLint/Prettier設定に従う
- import順・改行・命名は周辺コードに合わせる
- 未使用の変数/関数/importを残さない

## 5. テスト・検証
変更後は可能な限り以下を実行:
- `npm run lint`
- `npm run test`（存在する場合）
- `npm run build`（ビルド影響がある変更時）

実行できない項目がある場合は、理由をPR本文に明記する。

## 6. UI変更時のルール
- 画面表示に影響がある変更はスクリーンショットを添付する
- レイアウト崩れだけでなく、文言・導線変更点も説明する
- モバイル/デスクトップ両方の崩れを簡易確認する
- 導線悪化していないか確認する
- 文言、レイアウトの一貫性を確認する
- 学習継続を阻害しないか確認する
- 入力負荷が増えていないか確認する

## 7. データ/認証周り
- Amplify/Cognito関連の変更は破壊的変更を避ける
- 環境変数名や設定キーの変更時は移行手順をPRに記載する
- 秘密情報（キー・トークン等）をコミットしない

## 8. PR作成ルール
PR本文には最低限以下を含める:
1. 目的（Why）
2. 変更内容（What）
3. 確認手順（How to test）
4. 影響範囲（特に認証/データ/UI）
5. 未対応事項・既知の制約（あれば）

## 9. 禁止事項
- 無関係なファイルの整形
- 依存関係の無断追加
- コメントアウトによる死蔵コードの放置
- 機密情報のハードコード
- 指示されていないアーキテクチャ変更を行わない
- 大規模リファクタは明示許可がある場合のみ行う
- 既存UXを変更する場合は理由を説明する
- 推測で仕様を変更しない

## 10. コメント方針
- 新しく追加・変更する処理には、目的や判定理由が伝わる日本語コメントを付ける
- 自明な処理へのコメントは避ける
- 「なぜこの実装なのか」が分かりにくい箇所を優先してコメントする
- 一時対応・制約・将来改善予定がある場合は明記する
- 複雑な業務ロジックには概要コメントを付ける
- TODO コメントには期限または条件を添える
- UI文言から明確に読み取れる内容を重複コメントしない

## 11. 依存関係・lockfile方針
- build/install による incidental な package-lock.json の変更は原則 revert する
- lockfile を変更した場合は理由を明記する
- 依存追加時は理由を説明する

## 12. Git hooks運用
- commit前に pre-commit hook で `npm run lint` と `npm run typecheck` が自動実行される
- 初回セットアップ時は `git config core.hooksPath .githooks` を実行する
- hookを迂回する場合は、PR本文またはコミット説明に理由を明記する

## 13. Codex hooks運用
- Codexの編集後に repo-local hook で `npm run lint` と `npm run typecheck` が自動実行される
- hook失敗時は変更が自動revertされるわけではなく、次の修正対象として扱う
- 初回は `/hooks` で repo-local hook の trust が必要な場合がある
- hookが重い場合は無効化/調整してよいが、理由をPR本文または作業記録に明記する
